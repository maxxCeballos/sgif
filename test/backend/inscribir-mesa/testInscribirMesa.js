const assert = require("chai").assert;
const { Assertion } = require("chai");
const utlBackend = "localhost:3000"


describe('InscribirMesa', () => {
    describe('sayHello()', function () {
        it('app should return hello', function () {
            // let result = app.sayHello();
            assert.equal(1, 1);
        });
    })
})

const 

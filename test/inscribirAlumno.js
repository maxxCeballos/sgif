'use strict'

const { assert } = require('chai');
const chai = require('chai');

describe('Inscribir Alumno', function () {

    it('Debería responder fecha válida', function () {
        assert.equal(true,true)        
    }).timeout(0);
});

describe('Inscribir Alumno 2', function () {

    it('Debería responder fecha inválida', function () {
        assert.equal(false,false)
    })
});
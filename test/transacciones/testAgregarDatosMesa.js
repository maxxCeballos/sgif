const assert = require('chai').assert;




describe('Agregar Datos Mesa de Examen', function () {

  it('deberia devolver una lista de mesas en estado solicitada', function () {
    assert.equal(true, true);
  });


  it('deberia devolver una lista vacia', function () {
    //el segundo argumento es lo que deberia dar
    assert.equal(true, true);
  });
});